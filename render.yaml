services:
  - type: web
    name: scamshield
    runtime: python
    buildCommand: apt-get install -y tesseract-ocr libgl1 && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false